# Le dipendenze sono definite in pyproject.toml (unica fonte di verita').
# Installazione consigliata: `make setup-env` (gestisce anche la variante
# corretta di torch: MPS su Apple Silicon, CUDA se disponibile, CPU altrimenti).
-e .[dev]
